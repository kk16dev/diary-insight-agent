stack_name_base: diary-insight-agent

# Optional: Set to automatically create an admin user and email credentials
# If not provided, you'll need to manually create users via AWS Console
admin_user_email: null

backend:
  pattern: strands-single-agent # Available patterns: strands-single-agent, langgraph-single-agent
  deployment_type: zip # Available deployment types: docker (default), zip

# Phase 3: GitHub configuration for diary extraction
github:
  owner: kk16dev # GitHub repository owner
  repo: personal-knowledge-base # GitHub repository name (private repository)

# Resource tags for cost management and resource tracking (issue #6)
tags:
  Project: diary-insight-agent
  Environment: production
  ManagedBy: CDK
  Owner: kk16dev
